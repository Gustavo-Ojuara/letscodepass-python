idade = int(input("Digite a Idade: "))
if idade > 0 and idade < 150: #a. Idade: entre 0 e 150;
    print ("Idade no Intervalo (0-150)")
else:
    print ("Idade fora do Intervalo (0-150)")
salario = float(input("Digite o Salario: "))
if salario > 0: #b. Salário: maior que 0;
    print ("Salario maior que 0") 
else:
    print("Salario Errado")
sexo = input("Digite o Sexo: ")
if sexo == "M" or sexo == "F" or sexo == "Outro":  #c. Sexo: M, F ou Outro
    print("Sexo correto ")  
else:
    print("Sexo incorreto")
